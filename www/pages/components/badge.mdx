import Layout from "../../components/ComponentsLayout"

export const meta = {
    title: "Badge",
    description: "Show some badges",
    name: "@gladio/badge",
    usage:`import Badge from "@gladio/badge"
function App () {
  return <Badge>10</Badge>
}`
};

export default Layout

> Passing a `null` or `empty` values as children will render **NOT** render a Badge

## Playground

```jsx live=true
<Badge primary fullRounded>9</Badge>
```
