import Layout from "../../components/ComponentsLayout"
import ElementIcon from "./../../assets/undraw_elements.svg"

export const meta = {
    title: "Components",
    description: "Component library for Tourepedia Design.",
};

export default Layout

<div className="max-w-sm mx-auto my-8">
  <ElementIcon />
</div>

This package is composed of all other components and provides a compact way to use all other components from tourepedia.
Other components can be imported with there names only. Following components are exported:


`Button`, `Badge`, `BadgeList`, `ButtonGroup`, `ButtonToolbar`, `DateTime`, `DateTimePicker`, `DateTimeInput`, `Dialog`, `useDialog`,
`Icons`, `Paginate`, `Input`, `InputGroup`, `InputGroupAddon`, `FormGroup`, `TextArea`, `SelectInput`, `ErrorMessage`,
`Select`, `AsyncSelect`, `Table`

### Installation

```bash
npm install --save @tourepedia/ui
```

### CSS

CSS is included in `@tourepedia/ui/styles/index.css` that includes styles of all other components.

```css
@include @tourepedia/ui/styles/index;
```

### Usage

```jsx
import { Button } from "@tourepedia/ui"

function App () {
  return <Button branded>
    Save
  </Button>
}
```

### Playground

```jsx live=true
// import { Button, Badge, Dialog, Input, FormGroup, Select } from "@tourepedia/ui"

function App () {
const [isDialogOpen, changeDialogVisibility] = useState(false)
return <form onSubmit={(e) => { e.preventDefault(); changeDialogVisibility(true)}}>
  <FormGroup>
    <label>Name</label>
    <Input type="text" placeholder="Tourepedia Holidays" />
  </FormGroup>
  <FormGroup>
    <label>Select Email</label>
    <Select options={[{ id:1, name: "developer@tourepedia.com"}, {id: 2, name: "support@tourepedia.com" }]} />
  </FormGroup>
  <Dialog open={isDialogOpen} onClose={() => changeDialogVisibility(false)}>
    <Dialog.Body>
      Are you sure you want to continue ?
    </Dialog.Body>
    <Dialog.Footer>
    <Button danger primary>Continue</Button> <Button onClick={() => changeDialogVisibility(false)}>Cancel</Button>
    </Dialog.Footer>
  </Dialog>
  <Button type="submit">Save</Button>
</form>
}
```
